export function CheckCabValidator(driver) { 
    // let d = /^\d{10}$/;
    if (parseInt(driver) == 0 ) return "Please Add Your Cabs and get approved"
    // if (!mobile.match(d)) return 'Invalid mobile number'
    // if (mobile.length < 11 ) return 'Enter the correct mobile number'
    
    return ''
  }