export function bidamountValidator(amount) {
    // let d = /^\d{10}$/;
    if (!amount) return "Bid Amount can't be empty."
    // if (!mobile.match(d)) return 'Invalid mobile number'
    // if (mobile.length < 11 ) return 'Enter the correct mobile number'
    
    return ''
  }